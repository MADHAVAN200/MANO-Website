{
  "name": "mano-workspace",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "start": "npx --yes concurrently -k -n backend,frontend -c green,blue \"npm --prefix backend run start\" \"npm --prefix frontend run start\"",
    "dev": "npm run start",
    "backend": "npm --prefix backend run start",
    "frontend": "npm --prefix frontend run start",
    "install:all": "npm --prefix backend install && npm --prefix frontend install"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "kill-port": "^2.0.1"
  }
}
